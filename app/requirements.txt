# Dependencias de la app Streamlit de MetaCARP.
# (El paquete metacarp del proyecto ya debe ser importable desde la raíz.)
streamlit>=1.30
plotly>=5.18
networkx>=3.0
numpy>=1.24
pillow>=9.0
scipy>=1.10        # opcional: mejora el layout (kamada-kawai); si falta, usa spring
